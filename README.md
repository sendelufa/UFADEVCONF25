# Value Object в DDD: краткий конспект

> Материалы к выступлению на [UfaDevConf'25](https://dc.ufacoder.com/)

💬  [Приходите в группу DDD Community Ufa](https://t.me/+PAN4Us8cydowNWUy) для обсуждения не только темы доклада, но и для поиска решений вопросов
по DDD. Как реализовать, как решить возникшую проблему внедрения DDD.

## Что такое Value Object

**Value Object (VO)** — это объект, который определяется значением, а не идентичностью.
Два VO считаются одинаковыми, если совпадают все значимые поля. Для VO характерны:

- нет собственной идентичности (никакого `id` как у сущностей);
- иммутабельность: изменение = создание нового объекта;
- равенство по значениям, а не по ссылке;
- инварианты и валидация внутри самого типа (правильный email, корректные деньги и т.д.).

Главная идея: вместо «обезличенных» примитивов (`string`, `int`, `double`) вводить осмысленные доменные типы (`Weight`, `Email`, `Money`, `Quantity`, `Address`) и переносить в них логику и инварианты.

---

## Общие принципы использования VO

1. **Иммутабельность.**
   После создания состояние VO не меняется.

2. **Равенство по значениям.**
   Переопределённые `equals`/`hashCode`, `==`/`Equals`, `__eq__` и т.п. сравнивают поля, а не ссылки.

3. **Валидация при создании.**
   Невозможно создать «битый» VO (невалидный email, отрицательные деньги, пустой идентификатор, отрицательный вес).

4. **Локализация логики.**
   Мелкая доменная логика (форматирование, простые операции, проверки) живёт в VO, а не размазана по сервисам.

5. **Использование в публичных API.**
   В сигнатурах методов/функций/эндпоинтов лучше принимать/возвращать VO, а не примитивы.

---

## Языковые особенности и приёмы

### Java

#### Базовый шаблон

Классический VO в Java — это immutable-класс:

- `final` класс;
- `private final` поля;
- только геттеры;
- валидация в конструкторе или статических фабриках;
- `equals`/`hashCode`/`toString` по значимым полям.

```java
public record Weight(long grams) implements Comparable<Weight> {

    public Weight {
        if (grams < 0) {
            throw new IllegalArgumentException("Weight cannot be negative: %s g".formatted(grams));
        }
    }

    public static Weight ofKilograms(double kilograms) {
        var inGrams = BigDecimal.valueOf(kilograms)
                .multiply(BigDecimal.valueOf(1000));
        var rounded = inGrams.setScale(0, RoundingMode.HALF_UP);
        return new Weight(rounded.longValueExact());
    }

    public boolean isBetween(Weight first, Weight second) {
        var lowerBound = Math.min(first.grams, second.grams);
        var upperBound = Math.max(first.grams, second.grams);
        return grams >= lowerBound && grams <= upperBound;
    }

    @Override
    public int compareTo(Weight other) {
        return Long.compare(this.grams, other.grams);
    }
}
```

#### Records и библиотеки

- [`record` (Java 16+) отлично подходит для VO](https://skillbox.ru/media/base/chto-novogo-v-java-17/) — генерирует `equals`/`hashCode`/`toString`, поля final по умолчанию.
- Lombok `@Value`/`@With` для быстрого объявления immutable VO.
- Immutables, AutoValue — генерация value-типов и билдеров.
- [jMolecules](https://github.com/xmolecules/jmolecules) `@ValueObject` — пометка доменных VO и интеграция со Spring / JPA.

#### Оптимизации (Project Valhalla)

> https://openjdk.org/projects/valhalla/

Project Valhalla добавляет value-классы (inline/value objects) в JVM:

- объекты без идентичности с возможностью более плотного размещения в памяти;
- меньше аллокаций и лучше cache locality для коллекций VO.

Пока это экспериментальная история, но концептуально очень близка к DDD-VO
и показывает, что разработчики двигаются в сторону оптимизации при работе с VO.

---

### Kotlin

Kotlin из коробки сильно заточен под value-ориентированный стиль: есть `data class`, value-классы, `copy`, деструктуризация и хороший type inference.

#### Базовый подход: data class как VO

Для большинства VO достаточно `data class` с неизменяемыми свойствами:

```kotlin
data class Email private constructor(val value: String) {

    init {
        require("@" in value) { "Invalid email: $value" }
    }

    companion object {
        fun of(raw: String): Email = Email(raw.trim())
    }

    override fun toString(): String = value
}
```

Особенности:

- `data class` автоматически генерирует `equals`/`hashCode`/`toString` и `copy`.
- Свойства по умолчанию `val` (иммутабельность на уровне API).
- Можно прятать конструктор и делать фабричный метод `of/from`, чтобы централизовать валидацию.

#### Value-классы (`value class` / `@JvmInline`)

Для «тонких» обёрток над одним значением (идентификаторы, маленькие строки, числа) стоит использовать value-классы:

```kotlin
@JvmInline
value class Weight(val grams: Long) : Comparable<Weight> {

    init {
        require(grams >= 0) { "Weight cannot be negative: $grams g" }
    }

    fun isBetween(first: Weight, second: Weight): Boolean {
        val lower = minOf(first.grams, second.grams)
        val upper = maxOf(first.grams, second.grams)
        return grams in lower..upper
    }

    override fun compareTo(other: Weight): Int = grams.compareTo(other.grams)

    companion object {
        fun ofKilograms(kilograms: Double): Weight {
            val inGrams = BigDecimal.valueOf(kilograms).multiply(BigDecimal.valueOf(1000))
            val rounded = inGrams.setScale(0, RoundingMode.HALF_UP)
            return Weight(rounded.longValueExact())
        }
    }
}
```

Особенности value-классов:

- В байткоде и JIT они часто **инлайнится** до примитива/значения без дополнительной аллокации.
- Сохраняют типовую безопасность (`Wei` уже не перепутаешь со `String`).
- Есть некоторые ограничения (нельзя иметь `init` без конструктора, свои правила nullable-типов и generic-ов), но для VO обёрток над одним полем подходят идеально.

#### Библиотеки и подходы

- В доменном слое — `data class` и `value class` как основные строительные блоки VO.
- Kotlinx.serialization для прозрачной сериализации VO (часто достаточно обычного `@Serializable` поверх `data class` / value class).
- Arrow «добавляет мозгов» вокруг VO: `Either`, `Validated`, `NonEmptyList` и другие функциональные обёртки для выразительной валидации и композиции.

#### Оптимизации

- Для горячих структур данных (например, коллекции идентификаторов) value-классы существенно сокращают allocation rate.
- Для «составных» VO (`Money`, `Address`) разумнее использовать `data class`: выигрыша от инлайна всё равно не будет, а читаемость выше.
- Функции-расширения (`fun Email.domain(): String`) позволяют оставлять VO тонким, а поведение выносить вне, если он начинает раздуваться.

---

### C#

#### Основные варианты

В C# есть несколько естественных форм VO:

- `record` (reference type) — неизменяемый тип по умолчанию с value-based equality;
- `record struct` / `readonly record struct` — value-тип с семантикой VO;
- обычный immutable-класс с переопределёнными `Equals`/`GetHashCode`.

```cs
public readonly record struct Weight : IComparable<Weight>
{
    public long Grams { get; init; }

    public Weight(long grams)
    {
        if (grams < 0)
        {
            throw new ArgumentOutOfRangeException(nameof(grams), grams, "Weight cannot be negative");
        }

        Grams = grams;
    }

    public static Weight operator +(Weight left, Weight right) => new(left.Grams + right.Grams);

    public static Weight operator -(Weight left, Weight right)
    {
        var result = left.Grams - right.Grams;
        return new Weight(result);
    }

    public int CompareTo(Weight other) => Grams.CompareTo(other.Grams);
}
```

Частый подход — базовый абстрактный `ValueObject` с реализацией равенства и наследники, которые перечисляют значимые поля.

#### ORM и инфраструктура

- В EF Core VO мапятся как owned types.
- Immutable-типы требуют немного настроек (конструктор, конфигурация полей), но поддерживаются нормально.

#### Оптимизации

- `readonly record struct` уменьшает давление на GC в горячих участках кода.
- Маленькие VO (идентификаторы, флаги, небольшие агрегаты) удобно делать value-типами, большие — reference-type.

---

### Python

#### Dataclasses и attrs

В Python VO обычно делают через:

- `@dataclass(frozen=True)` — иммутабельный dataclass с равенством по полям;
- `slots=True` (Python 3.10+) или явный `__slots__` для уменьшения расхода памяти.

```python
from dataclasses import dataclass

@dataclass(frozen=True, order=True, slots=True)
class Weight:
    """Value Object that stores weight in grams and exposes helpers for comparisons."""

    grams: int

    def __post_init__(self) -> None:
        if not isinstance(self.grams, int):
            raise TypeError(f"Weight must be an integer, got {type(self.grams)}")
        if self.grams < 0:
            raise ValueError(f"Weight cannot be negative: {self.grams} g")
```

Альтернатива — библиотека **attrs** (`@attr.s(frozen=True)`) с ещё более гибкой конфигурацией и встроенной валидацией.

#### Pydantic и пограничные модели

На границах системы (HTTP, message bus, конфиги) часто используют **Pydantic**:

- валидирует входные данные;
- может генерировать immutabe-модели (`Config` / `model_config`);
- удобно комбинируется с dataclasses.

Внутри домена обычно достаточно `frozen dataclass`/attrs-классов.

#### Оптимизации

- `slots=True` и attrs (`slots=True`) заметно уменьшают overhead VO при большом количестве объектов.
- Pydantic — удобство в обмен на CPU, его лучше держать на boundary-слое, а не в самом горячем доменном коде.

---

### Go

Go изначально value-ориентирован, поэтому VO ложится довольно естественно.

#### Базовый паттерн

- `struct` с неэкспортируемыми полями;
- конструктор `NewXxx` с валидацией;
- без сеттеров, только методы чтения или методы, возвращающие новые значения.

```go
type Weight struct {
    grams int64
}

func NewWeight(grams int64) (Weight, error) {
    if grams < 0 {
        return Weight{}, fmt.Errorf("weight cannot be negative: %d g", grams)
    }
    return Weight{grams: grams}, nil
}
```

#### Особенности

- Копирование struct по значению по умолчанию даёт value semantics.
- Для маленьких VO (пара полей) это дёшево; для больших можно использовать указатели, не нарушая логическую иммутабельность.

#### Оптимизации

- Компилятор часто размещает мелкие VO на стеке, если они не убегают в кучу.
- Стоит следить, чтобы VO были компактными (без лишних указателей и больших срезов внутри).

---

## Чего лучше избегать

- Сеттеры и мутабельное состояние внутри VO.
- Протаскивание VO наружу в виде примитивов («давайте вернём просто string вместо Email»).
- «Псевдо-VO» без инвариантов (обёрнули `String`, но не валидируете — пользы мало).

## Материалы

💬  [Приходите в группу DDD Community Ufa](https://t.me/+PAN4Us8cydowNWUy) для обсуждения не только темы доклада, но и для поиска решений вопросов
по DDD. Как реализовать, как решить возникшую проблему внедрения DDD.

1. 📼 Kotlin/Java [StringConcat — VALUE OBJECTS - первый шаг к чистому коду](https://youtu.be/XFCbV4mWHLo?si=ybEaZ_RlD_ktms5c)
1. 📼 Kotlin/Java [StringConcat — Коллекции VALUE OBJECTS — ещё ближе к совершенству](https://youtu.be/ry-VaAqW2Xk?si=OQf8R0ZtTC0vXed2)
1. 📼 C# [Milan Jovanović — How to Use Value Objects to Solve Primitive Obsession](https://youtu.be/P5CRea21R2E?si=Rq85IDMKHjROXtJc)
1. 📃 Go [Dennis Vis — Domain-driven Design in Go: Value Objects](https://dennisvis.dev/blog/ddd-in-go-value-objects)
1. 📃 Go [Marko Milojevic  — Practical DDD in Golang: Value Object](https://www.ompluscator.com/article/golang/practical-ddd-value-object/)
1. 📃 [Martin Fowler — Value Object](https://martinfowler.com/bliki/ValueObject.html)
1. 📃 [Vladimir Khorikov — Value Objects explained](https://enterprisecraftsmanship.com/posts/value-objects-explained/)
